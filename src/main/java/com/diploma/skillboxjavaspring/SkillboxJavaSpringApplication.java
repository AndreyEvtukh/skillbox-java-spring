package com.diploma.skillboxjavaspring;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the hotel booking application.
 */
@SpringBootApplication
public class SkillboxJavaSpringApplication {

    /**
     * Starts the Spring application context.
     *
     * @param args command-line arguments passed to Spring Boot
     */
    static void main(String[] args) {
        SpringApplication.run(SkillboxJavaSpringApplication.class, args);
    }

}
